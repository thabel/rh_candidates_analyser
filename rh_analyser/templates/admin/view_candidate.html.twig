<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Candidat - Détails</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: #f5f5f5;
        }
        header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        header h1 { font-size: 24px; }
        .back-btn {
            background: rgba(255,255,255,0.2);
            padding: 8px 16px;
            border-radius: 5px;
            text-decoration: none;
            color: white;
            font-size: 14px;
        }
        .back-btn:hover { background: rgba(255,255,255,0.3); }
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }
        .grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin-bottom: 20px;
        }
        .card {
            background: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        }
        .card h2 {
            color: #333;
            margin-bottom: 15px;
            border-bottom: 2px solid #667eea;
            padding-bottom: 10px;
        }
        .info-item {
            margin-bottom: 12px;
        }
        .info-label {
            color: #666;
            font-size: 13px;
            font-weight: 600;
            text-transform: uppercase;
        }
        .info-value {
            color: #333;
            font-size: 15px;
            margin-top: 3px;
        }
        .status-badge {
            display: inline-block;
            padding: 6px 12px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 600;
        }
        .status-pending { background: #fff3cd; color: #856404; }
        .status-analyzing { background: #cfe2ff; color: #084298; }
        .status-analyzed { background: #d1e7dd; color: #0f5132; }
        .cv-text {
            background: #f9f9f9;
            padding: 15px;
            border-radius: 5px;
            max-height: 400px;
            overflow-y: auto;
            white-space: pre-wrap;
            word-wrap: break-word;
            font-size: 13px;
            line-height: 1.6;
            border-left: 3px solid #667eea;
        }
        .analyze-form {
            display: flex;
            flex-direction: column;
        }
        .form-group {
            margin-bottom: 15px;
        }
        .form-group label {
            color: #333;
            font-weight: 600;
            margin-bottom: 8px;
            display: block;
        }
        .form-group textarea {
            width: 100%;
            padding: 12px;
            border: 1px solid #ddd;
            border-radius: 5px;
            font-family: inherit;
            font-size: 14px;
            resize: vertical;
            min-height: 150px;
        }
        .form-group textarea:focus {
            outline: none;
            border-color: #667eea;
            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
        }
        .btn {
            padding: 12px;
            border: none;
            border-radius: 5px;
            font-weight: bold;
            cursor: pointer;
            font-size: 14px;
            transition: all 0.3s;
        }
        .btn-analyze {
            background: #28a745;
            color: white;
            margin-bottom: 10px;
        }
        .btn-analyze:hover { background: #218838; }
        .btn-delete {
            background: #dc3545;
            color: white;
        }
        .btn-delete:hover { background: #c82333; }
        .analysis-result {
            grid-column: 1 / -1;
        }
        .score-display {
            font-size: 48px;
            font-weight: bold;
            color: #667eea;
            text-align: center;
            margin: 20px 0;
        }
        .analysis-section {
            margin-bottom: 20px;
        }
        .analysis-section h3 {
            color: #333;
            margin-bottom: 10px;
            font-size: 15px;
        }
        .analysis-item {
            padding: 12px;
            background: #f9f9f9;
            border-radius: 5px;
            margin-bottom: 8px;
            border-left: 3px solid #667eea;
            font-size: 14px;
            line-height: 1.5;
        }
        .analysis-item.positive {
            border-left-color: #28a745;
            background: #f0f9f6;
        }
        .analysis-item.negative {
            border-left-color: #dc3545;
            background: #fdf7f7;
        }
        .alerts {
            margin-bottom: 20px;
        }
        .alert {
            padding: 15px;
            border-radius: 5px;
            margin-bottom: 10px;
        }
        .alert.success {
            background: #d4edda;
            color: #155724;
            border: 1px solid #c3e6cb;
        }
        .alert.error {
            background: #f8d7da;
            color: #721c24;
            border: 1px solid #f5c6cb;
        }
        @media (max-width: 768px) {
            .grid {
                grid-template-columns: 1fr;
            }
            .analysis-result {
                grid-column: 1;
            }
        }
    </style>
</head>
<body>
    <header>
        <h1>{{ candidate.firstName }} {{ candidate.lastName }}</h1>
        <a href="{{ path('admin_dashboard') }}" class="back-btn">← Retour</a>
    </header>

    <div class="container">
        <div class="alerts">
            {% for message in app.flashes('success') %}
                <div class="alert success">✅ {{ message }}</div>
            {% endfor %}
            {% for message in app.flashes('error') %}
                <div class="alert error">❌ {{ message }}</div>
            {% endfor %}
        </div>

        <div class="grid">
            <div class="card">
                <h2>📋 Informations</h2>
                <div class="info-item">
                    <div class="info-label">Email</div>
                    <div class="info-value">{{ candidate.email }}</div>
                </div>
                <div class="info-item">
                    <div class="info-label">Statut</div>
                    <div class="info-value">
                        <span class="status-badge status-{{ candidate.status }}">
                            {% if candidate.status == 'pending' %}
                                ⏳ En attente
                            {% elseif candidate.status == 'analyzing' %}
                                ⚙️ Analyse...
                            {% else %}
                                ✅ Analysée
                            {% endif %}
                        </span>
                    </div>
                </div>
                <div class="info-item">
                    <div class="info-label">Soumise</div>
                    <div class="info-value">{{ candidate.submittedAt.format('d/m/Y H:i') }}</div>
                </div>
                {% if candidate.analyzedAt %}
                    <div class="info-item">
                        <div class="info-label">Analysée</div>
                        <div class="info-value">{{ candidate.analyzedAt.format('d/m/Y H:i') }}</div>
                    </div>
                {% endif %}
                {% if candidate.score %}
                    <div class="info-item">
                        <div class="info-label">Score</div>
                        <div class="info-value" style="font-size: 24px; color: #667eea; font-weight: bold;">
                            {{ candidate.score }}/100
                        </div>
                    </div>
                {% endif %}
            </div>

            <div class="card">
                <h2>📝 CV</h2>
                <div class="cv-text">{{ candidate.cvText }}</div>
            </div>

            {% if candidate.status != 'analyzed' %}
                <div class="card">
                    <h2>🔍 Analyser Avec Gemini</h2>
                    <form method="POST" action="{{ path('admin_analyze_candidate_web', {id: candidate.id}) }}" class="analyze-form">
                        <div class="form-group">
                            <label for="jobDescription">Fiche de Poste (Job Description)</label>
                            <textarea id="jobDescription" name="jobDescription" required placeholder="Décrivez le poste, les compétences requises, l'expérience..."></textarea>
                        </div>
                        <button type="submit" class="btn btn-analyze">🚀 Lancer l'Analyse</button>
                    </form>
                </div>
            {% endif %}

            {% if candidate.analysisResult %}
                <div class="card analysis-result">
                    <h2>📊 Résultats d'Analyse</h2>
                    <div class="score-display">{{ candidate.score }}/100</div>

                    <div class="analysis-section">
                        <h3>📌 Résumé</h3>
                        <div class="analysis-item">
                            {{ candidate.analysisResult.summary }}
                        </div>
                    </div>

                    <div class="analysis-section">
                        <h3>✅ Points Forts</h3>
                        {% for point in candidate.analysisResult.positives %}
                            <div class="analysis-item positive">
                                • {{ point }}
                            </div>
                        {% endfor %}
                    </div>

                    <div class="analysis-section">
                        <h3>⚠️ Points à Améliorer</h3>
                        {% for point in candidate.analysisResult.negatives %}
                            <div class="analysis-item negative">
                                • {{ point }}
                            </div>
                        {% endfor %}
                    </div>
                </div>
            {% endif %}

            <div class="card" style="grid-column: 1 / -1;">
                <form method="POST" action="{{ path('admin_delete_candidate', {id: candidate.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette candidature?');">
                    <button type="submit" class="btn btn-delete">🗑️ Supprimer</button>
                </form>
            </div>
        </div>
    </div>
</body>
</html>
