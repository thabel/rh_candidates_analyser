<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tableau de Bord Admin - RH</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
        }
        header {
            background: rgba(0,0,0,0.1);
            color: white;
            padding: 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            backdrop-filter: blur(10px);
        }
        header h1 { font-size: 28px; }
        .logout {
            background: rgba(255,255,255,0.2);
            padding: 10px 18px;
            border-radius: 5px;
            text-decoration: none;
            color: white;
            font-size: 14px;
        }
        .logout:hover { background: rgba(255,255,255,0.3); }
        .container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 30px 20px;
        }
        .stats-header {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 20px;
            margin-bottom: 40px;
        }
        .stat-card {
            background: white;
            padding: 25px;
            border-radius: 12px;
            box-shadow: 0 8px 32px rgba(0,0,0,0.1);
            text-align: center;
        }
        .stat-card .number {
            font-size: 36px;
            font-weight: bold;
            color: #667eea;
            margin: 10px 0;
        }
        .stat-card .label {
            color: #666;
            font-size: 13px;
            text-transform: uppercase;
            font-weight: 600;
            letter-spacing: 0.5px;
        }
        .section-title {
            color: white;
            font-size: 24px;
            margin-bottom: 20px;
            font-weight: 600;
        }
        .jobs-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 20px;
            margin-bottom: 40px;
        }
        .job-card {
            background: white;
            border-radius: 12px;
            box-shadow: 0 8px 32px rgba(0,0,0,0.1);
            overflow: hidden;
            cursor: pointer;
            transition: all 0.3s;
            text-decoration: none;
            color: inherit;
            display: flex;
            flex-direction: column;
        }
        .job-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 12px 48px rgba(0,0,0,0.15);
        }
        .job-card-header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 20px;
        }
        .job-card-header h3 {
            font-size: 18px;
            margin-bottom: 8px;
        }
        .job-card-header p {
            font-size: 12px;
            opacity: 0.9;
            line-height: 1.5;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }
        .job-card-body {
            padding: 20px;
            flex: 1;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }
        .job-stats {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 10px;
            margin-bottom: 15px;
        }
        .job-stat {
            text-align: center;
        }
        .job-stat .number {
            font-size: 20px;
            font-weight: bold;
            color: #333;
        }
        .job-stat .label {
            font-size: 11px;
            color: #999;
            text-transform: uppercase;
            font-weight: 600;
        }
        .job-stat.pending .number { color: #ffc107; }
        .job-stat.analyzed .number { color: #28a745; }
        .view-btn {
            background: #667eea;
            color: white;
            border: none;
            padding: 10px 16px;
            border-radius: 6px;
            cursor: pointer;
            font-weight: 600;
            font-size: 13px;
            width: 100%;
            transition: all 0.3s;
        }
        .view-btn:hover {
            background: #764ba2;
            transform: translateY(-2px);
        }
        /* Candidate List View */
        .candidates-view {
            background: white;
            border-radius: 12px;
            box-shadow: 0 8px 32px rgba(0,0,0,0.1);
            overflow: hidden;
        }
        .candidates-header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 25px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .candidates-header h2 {
            font-size: 22px;
        }
        .candidates-header p {
            font-size: 13px;
            opacity: 0.9;
            margin-top: 5px;
        }
        .back-btn {
            background: rgba(255,255,255,0.2);
            color: white;
            border: none;
            padding: 10px 16px;
            border-radius: 5px;
            cursor: pointer;
            font-weight: 600;
        }
        .back-btn:hover { background: rgba(255,255,255,0.3); }
        .filter-bar {
            padding: 20px 25px;
            background: #f9f9f9;
            border-bottom: 1px solid #eee;
            display: flex;
            gap: 10px;
            flex-wrap: wrap;
        }
        .filter-btn {
            padding: 8px 16px;
            border: 1px solid #ddd;
            background: white;
            border-radius: 5px;
            cursor: pointer;
            font-size: 13px;
            font-weight: 600;
            transition: all 0.3s;
        }
        .filter-btn:hover {
            background: #f0f0f0;
        }
        .filter-btn.active {
            background: #667eea;
            color: white;
            border-color: #667eea;
        }
        .candidates-list {
            padding: 0;
        }
        .candidate-item {
            padding: 20px 25px;
            border-bottom: 1px solid #f0f0f0;
            display: flex;
            align-items: center;
            justify-content: space-between;
            transition: background 0.2s;
        }
        .candidate-item:hover {
            background: #f9f9f9;
        }
        .candidate-item:last-child {
            border-bottom: none;
        }
        .candidate-info {
            flex: 1;
        }
        .candidate-name {
            font-weight: 600;
            color: #333;
            font-size: 15px;
            margin-bottom: 3px;
        }
        .candidate-email {
            font-size: 13px;
            color: #999;
        }
        .candidate-meta {
            display: flex;
            gap: 25px;
            align-items: center;
        }
        .status-badge {
            padding: 6px 12px;
            border-radius: 20px;
            font-size: 11px;
            font-weight: 700;
            text-transform: uppercase;
        }
        .status-pending { background: #fff3cd; color: #856404; }
        .status-analyzing { background: #cfe2ff; color: #084298; }
        .status-analyzed { background: #d1e7dd; color: #0f5132; }
        .score {
            font-weight: bold;
            min-width: 60px;
            text-align: center;
        }
        .score.high { color: #28a745; }
        .score.low { color: #dc3545; }
        .score.none { color: #999; }
        .view-candidate-btn {
            background: #667eea;
            color: white;
            border: none;
            padding: 8px 14px;
            border-radius: 5px;
            cursor: pointer;
            font-size: 12px;
            font-weight: 600;
            transition: all 0.3s;
        }
        .view-candidate-btn:hover {
            background: #764ba2;
        }
        .batch-analyze-btn {
            background: #28a745;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
            font-weight: 600;
            font-size: 13px;
        }
        .batch-analyze-btn:hover {
            background: #218838;
        }
        .empty-state {
            text-align: center;
            padding: 60px 20px;
            color: #999;
        }
        .empty-state p {
            font-size: 16px;
            margin-bottom: 10px;
        }
        .alerts {
            margin-bottom: 20px;
        }
        .alert {
            padding: 12px 20px;
            border-radius: 6px;
            margin-bottom: 10px;
            font-weight: 500;
        }
        .alert.success {
            background: #d1e7dd;
            color: #0f5132;
            border-left: 4px solid #28a745;
        }
        .alert.error {
            background: #f8d7da;
            color: #721c24;
            border-left: 4px solid #dc3545;
        }
        @media (max-width: 768px) {
            .stats-header {
                grid-template-columns: 1fr;
            }
            .jobs-grid {
                grid-template-columns: 1fr;
            }
            .candidate-meta {
                flex-direction: column;
                align-items: flex-start;
                gap: 8px;
            }
        }
    </style>
</head>
<body>
    <header>
        <h1>📊 Tableau de Bord RH</h1>
        <a href="{{ path('admin_logout') }}" class="logout">🚪 Déconnexion</a>
    </header>

    <div class="container">
        <div class="alerts">
            {% for message in app.flashes('success') %}
                <div class="alert success">✅ {{ message }}</div>
            {% endfor %}
            {% for message in app.flashes('error') %}
                <div class="alert error">❌ {{ message }}</div>
            {% endfor %}
        </div>

        <!-- Statistics -->
        <div class="stats-header">
            <div class="stat-card">
                <div class="label">📋 Total Candidats</div>
                <div class="number">{{ totalCandidates }}</div>
            </div>
            <div class="stat-card">
                <div class="label">⏳ En Attente</div>
                <div class="number" style="color: #ffc107;">{{ totalPending }}</div>
            </div>
            <div class="stat-card">
                <div class="label">✅ Analysés</div>
                <div class="number" style="color: #28a745;">{{ totalAnalyzed }}</div>
            </div>
        </div>

        <!-- Job Selection or Candidate List -->
        {% if selectedJobId %}
            <!-- CANDIDATE LIST VIEW -->
            <div class="candidates-view">
                <div class="candidates-header">
                    <div>
                        <h2>{{ currentJob.title }}</h2>
                        <p>{{ currentJob.description|slice(0, 150) }}...</p>
                    </div>
                    <form method="POST" action="{{ path('admin_analyze_batch', {jobId: currentJob.id}) }}" style="margin-left: auto;">
                        {% if jobPendingCount > 0 %}
                            <button type="submit" class="batch-analyze-btn">🚀 Analyser {{ jobPendingCount }} Candidats</button>
                        {% endif %}
                    </form>
                    <a href="{{ path('admin_dashboard') }}" class="back-btn" style="margin-left: 15px;">← Retour</a>
                </div>

                <div class="filter-bar">
                    <strong style="color: #667eea;">Filtrer:</strong>
                    <a href="?filter=all" class="filter-btn {% if currentFilter == 'all' %}active{% endif %}">Tous ({{ totalJobCandidates }})</a>
                    <a href="?filter=pending" class="filter-btn {% if currentFilter == 'pending' %}active{% endif %}">⏳ En Attente ({{ jobPendingCount }})</a>
                    <a href="?filter=analyzed" class="filter-btn {% if currentFilter == 'analyzed' %}active{% endif %}">✅ Analysés ({{ jobAnalyzedCount }})</a>
                </div>

                <div class="candidates-list">
                    {% if candidatesByJob|length > 0 %}
                        {% for candidate in candidatesByJob %}
                            <div class="candidate-item">
                                <div class="candidate-info">
                                    <div class="candidate-name">{{ candidate.firstName }} {{ candidate.lastName }}</div>
                                    <div class="candidate-email">{{ candidate.email }}</div>
                                </div>
                                <div class="candidate-meta">
                                    <span class="status-badge status-{{ candidate.status }}">
                                        {% if candidate.status == 'pending' %}
                                            ⏳ En attente
                                        {% elseif candidate.status == 'analyzing' %}
                                            ⚙️ Analyse...
                                        {% else %}
                                            ✅ Analysée
                                        {% endif %}
                                    </span>
                                    <div class="score {% if candidate.score >= 80 %}high{% elseif candidate.score %}low{% else %}none{% endif %}">
                                        {% if candidate.score %}
                                            {{ candidate.score }}/100
                                        {% else %}
                                            -
                                        {% endif %}
                                    </div>
                                    <a href="{{ path('admin_view_candidate', {id: candidate.id}) }}" class="view-candidate-btn">👁️ Voir</a>
                                </div>
                            </div>
                        {% endfor %}
                    {% else %}
                        <div class="empty-state">
                            <p>Aucune candidature pour cette offre.</p>
                        </div>
                    {% endif %}
                </div>
            </div>
        {% else %}
            <!-- JOB SELECTION VIEW -->
            <div class="section-title">👇 Sélectionnez une offre d'emploi</div>
            <div class="jobs-grid">
                {% for job in jobs %}
                    <div class="job-card">
                        <div class="job-card-header">
                            <h3>{{ job.title }}</h3>
                            <p>{{ job.description|slice(0, 80) }}...</p>
                        </div>
                        <div class="job-card-body">
                            <div class="job-stats">
                                <div class="job-stat">
                                    <div class="number">{{ jobStats[job.id].total }}</div>
                                    <div class="label">Total</div>
                                </div>
                                <div class="job-stat pending">
                                    <div class="number">{{ jobStats[job.id].pending }}</div>
                                    <div class="label">Attente</div>
                                </div>
                                <div class="job-stat analyzed">
                                    <div class="number">{{ jobStats[job.id].analyzed }}</div>
                                    <div class="label">Analysés</div>
                                </div>
                            </div>
                            <a href="?job={{ job.id }}" class="view-btn">Voir Candidats →</a>
                        </div>
                    </div>
                {% endfor %}
            </div>

            {% if jobs|length == 0 %}
                <div class="empty-state">
                    <p>📋 Aucune offre d'emploi créée.</p>
                </div>
            {% endif %}
        {% endif %}
    </div>
</body>
</html>
